Fra en anden pc som skal have "setliste-app" over på den, kør flg i terminalen:

1. Hent koden:
Bash

git clone https://github.com/Ulreg/min-setliste-app.git
cd min-setliste-app

2. Installer alt det nødvendige (Electron osv.):
Bash

npm install

3. Byg appen til Linux:
Bash

npm run dist
